# Instruction
  # @feed_items 
    # - user.rb/feed Method/Rails Active Record/where Method's returning Variable.
	    # - When Rails/ActiveRecord/where Method Fetches data from the Database, it makes an Array of FETCHED Data which can be accessed as VARIABLE_items in the View.

<% if @blog_posts_feed.any? %>
  <ol class="blogs">
    <%= render @blog_posts_feed %>
  </ol> 
   <%= will_paginate @blog_posts_feed,
                    params: { controller: :static_pages, action: :home } 
                    %>
<% end %>
